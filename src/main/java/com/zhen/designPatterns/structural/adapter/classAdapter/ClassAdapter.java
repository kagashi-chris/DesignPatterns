package com.zhen.designPatterns.structural.adapter.classAdapter;

public class ClassAdapter {

    //class adapter is done through multiple inheritance (extend)
    //this type of adapt is not possible in java because java only support single inheritance.
}
